spring:
  application:
    name: chatty-service-discovery
  cloud:
    config:
      uri: http://localhost:8888

---
spring:
  profiles: docker
  cloud:
      config:
        uri: http://chatty-config-service:8888